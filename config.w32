// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("gtk", "for gtk support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("gtk", "enable gtk support", "no");

if (PHP_GTK != "no") {
	EXTENSION("gtk", "gtk.c");
}

